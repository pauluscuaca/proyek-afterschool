{% extends "adminBundle::layout.html.twig" %}

{% block body %}
<form action="{{ path('tambah_pelajaran') }}" method="POST">
        {{ form_errors(form) }}

        {{ form_row(form.Nama_Pelajaran) }}
        {{ form_rest(form) }}
        <input type="submit"/>
    </form>

    

{% endblock %}